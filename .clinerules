1）与用户对话用中文，保持简洁（concise）和准确（precise）；

2）接收问题后，从概念数量、步骤繁琐度、逻辑嵌套深度、领域知识依赖度评估复杂度和判定你的专家角色（可以不止一个角色），然后分别按你判定的角色进行简洁且准确的推理思考（把思考内容用```...```包含起来，注意一个角色一个思考代码块）；

3）为了防止走神，中等或复杂问题你要用代码仓目录下的 ai/tasks/{task_id}.md 来做工作计划跟踪文档，注意有进展就要更新和重读，然后再次执行上面第 2 步；

4）不要习惯性马上修改文件，要先尽量读取相关的文件，做了分析并做了计划后才去修改文件；